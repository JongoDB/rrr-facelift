/**
 * Phase 00 placeholder. Replaced in Phase 03 when Astro is initialised.
 * Keeping a real, type-checkable export so the workspace is buildable in CI.
 */
export const PHASE_00_WEB_SCAFFOLD = true as const;
